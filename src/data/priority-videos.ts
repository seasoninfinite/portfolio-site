/**
 * First two capability showcase loops — preloaded during splash; keep order in sync with
 * `CapabilitiesStorySection` (hero-adjacent content when scrolling).
 */
export const PRIORITY_CAPABILITY_VIDEO_SRCS = [
  "/work/loops/logoipsum.mp4",
  "/work/loops/logoipsum2.mp4",
] as const;
