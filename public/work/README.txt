WHERE TO PUT YOUR FILES (same folder as this file)

1) VIDEO LOOPS — public/work/loops/
   Filenames are listed in src/data/work-content.ts (animatedLoopVideos).
   Current set includes: logoipsum.mp4, logoipsum2.mp4, logoipsum3.mp4, targonet,
   novaforge, flyro, powerai.mp4, venture, grow, apex, brandly, evr, terra.

2) SCREENSHOTS — public/work/screenshots/
   PNG filenames and live links are in src/data/work-content.ts (staticWorkItems).

If a file is missing, the site still runs (video placeholder or image fallback).
